<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Aswanidev VS | Backend Architect</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500;600&family=Inter:wght@300;400;600;800&display=swap" rel="stylesheet">
  
  <style>
    /* --- VARIABLES & RESET --- */
    :root {
      --bg-color: #050507;
      --bg-secondary: #0f0f13;
      --accent-primary: #00ff9d; /* Neon Mint */
      --accent-secondary: #00b8ff; /* Cyan */
      --text-main: #ffffff;
      --text-muted: #a1a1aa;
      --border-color: rgba(255, 255, 255, 0.1);
      --card-bg: rgba(20, 20, 25, 0.6);
      --glass-blur: blur(12px);
      --font-main: 'Inter', sans-serif;
      --font-code: 'Fira Code', monospace;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      background-color: var(--bg-color);
      color: var(--text-main);
      font-family: var(--font-main);
      line-height: 1.6;
      overflow-x: hidden;
      min-height: 100vh;
      position: relative;
    }

    /* Ambient Background Glow */
    body::before {
      content: '';
      position: fixed;
      top: -20%;
      left: 20%;
      width: 60vw;
      height: 60vw;
      background: radial-gradient(circle, rgba(0, 255, 157, 0.07) 0%, transparent 60%);
      z-index: -1;
      pointer-events: none;
    }
    
    body::after {
      content: '';
      position: fixed;
      bottom: -10%;
      right: -10%;
      width: 40vw;
      height: 40vw;
      background: radial-gradient(circle, rgba(0, 184, 255, 0.05) 0%, transparent 60%);
      z-index: -1;
      pointer-events: none;
    }

    a { text-decoration: none; color: inherit; transition: 0.3s; }
    ul { list-style: none; }

    /* --- LAYOUT UTILITIES --- */
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 2rem;
    }

    /* --- HEADER --- */
    header {
      position: fixed;
      top: 0;
      width: 100%;
      z-index: 1000;
      padding: 1.25rem 0;
      border-bottom: 1px solid var(--border-color);
      background: rgba(5, 5, 7, 0.8);
      backdrop-filter: var(--glass-blur);
    }

    .nav-wrapper {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-family: var(--font-code);
      font-weight: 700;
      font-size: 1.25rem;
      letter-spacing: -0.5px;
      position: relative;
    }

    .logo span { color: var(--accent-primary); }

    /* Glitch Hover Effect for Logo */
    .logo:hover {
      text-shadow: 2px 0 var(--accent-secondary), -2px 0 var(--accent-primary);
    }

    nav {
      display: flex;
      gap: 2rem;
      align-items: center;
    }

    nav a {
      font-size: 0.9rem;
      color: var(--text-muted);
      font-weight: 500;
    }

    nav a:hover { color: var(--accent-primary); }

    .btn-nav {
      padding: 0.5rem 1.25rem;
      border: 1px solid var(--border-color);
      border-radius: 6px;
      color: var(--text-main);
      font-size: 0.85rem;
    }

    .btn-nav:hover {
      border-color: var(--accent-primary);
      background: rgba(0, 255, 157, 0.1);
      color: var(--accent-primary);
    }

    /* --- HERO SECTION --- */
    .hero {
      padding: 10rem 0 6rem;
      display: grid;
      grid-template-columns: 1.2fr 1fr;
      gap: 4rem;
      align-items: center;
    }

    .hero-content h1 {
      font-size: clamp(2.5rem, 5vw, 4rem);
      font-weight: 800;
      line-height: 1.1;
      margin-bottom: 1.5rem;
      background: linear-gradient(to right, #fff, #a1a1aa);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }

    .role-badge {
      display: inline-block;
      font-family: var(--font-code);
      font-size: 0.85rem;
      color: var(--accent-primary);
      background: rgba(0, 255, 157, 0.1);
      padding: 0.4rem 0.8rem;
      border-radius: 4px;
      margin-bottom: 1.5rem;
      border: 1px solid rgba(0, 255, 157, 0.2);
    }

    .hero-subtitle {
      color: var(--text-muted);
      font-size: 1.1rem;
      margin-bottom: 2.5rem;
      max-width: 500px;
    }

    .cta-group {
      display: flex;
      gap: 1rem;
    }

    .btn-primary {
      background: var(--text-main);
      color: var(--bg-color);
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      font-weight: 600;
      font-size: 0.95rem;
    }

    .btn-primary:hover {
      background: var(--accent-primary);
      transform: translateY(-2px);
      box-shadow: 0 4px 20px rgba(0, 255, 157, 0.3);
    }

    .btn-outline {
      border: 1px solid var(--border-color);
      color: var(--text-main);
      padding: 0.8rem 1.5rem;
      border-radius: 6px;
      font-weight: 500;
    }

    .btn-outline:hover {
      border-color: var(--text-main);
      background: rgba(255,255,255,0.05);
    }

    /* Hero Visual (Fake Terminal) */
    .terminal-window {
      background: #0d0d10;
      border: 1px solid var(--border-color);
      border-radius: 10px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.5);
      overflow: hidden;
      font-family: var(--font-code);
      font-size: 0.85rem;
      position: relative;
    }

    .terminal-header {
      background: #1a1a1e;
      padding: 10px 15px;
      display: flex;
      gap: 8px;
      border-bottom: 1px solid var(--border-color);
    }

    .dot { width: 10px; height: 10px; border-radius: 50%; }
    .red { background: #ff5f56; }
    .yellow { background: #ffbd2e; }
    .green { background: #27c93f; }

    .terminal-body {
      padding: 20px;
      color: var(--text-muted);
    }

    .code-line { margin-bottom: 8px; display: block; }
    .kw { color: #c678dd; } /* keyword */
    .str { color: #98c379; } /* string */
    .fn { color: #61afef; } /* function */
    .comment { color: #5c6370; font-style: italic; }
    
    .cursor {
      display: inline-block;
      width: 8px;
      height: 15px;
      background: var(--accent-primary);
      animation: blink 1s infinite;
      vertical-align: middle;
    }

    @keyframes blink { 50% { opacity: 0; } }

    /* --- TECH STACK --- */
    .tech-scroller {
      border-top: 1px solid var(--border-color);
      border-bottom: 1px solid var(--border-color);
      padding: 2rem 0;
      background: rgba(255, 255, 255, 0.02);
      margin-bottom: 4rem;
    }

    .tech-list {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 2rem;
      opacity: 0.7;
    }

    .tech-item {
      font-family: var(--font-code);
      font-size: 0.9rem;
      color: var(--text-muted);
      display: flex;
      align-items: center;
      gap: 8px;
    }
    
    .tech-item::before {
      content: '#';
      color: var(--accent-primary);
    }

    /* --- PROJECTS SECTION --- */
    .section-title {
      font-size: 1.8rem;
      margin-bottom: 3rem;
      display: flex;
      align-items: center;
      gap: 1rem;
    }
    
    .section-title::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border-color);
    }

    /* UPDATED: Card is now an anchor tag block */
    .project-card {
      display: block; /* Important for anchor tags */
      background: var(--card-bg);
      border: 1px solid var(--border-color);
      border-radius: 12px;
      padding: 2.5rem;
      transition: 0.4s ease;
      position: relative;
      overflow: hidden;
      text-decoration: none; /* Remove underline */
      color: inherit; /* Prevent blue text */
      cursor: pointer;
    }

    .project-card:hover {
      transform: translateY(-5px);
      border-color: rgba(0, 255, 157, 0.3);
      box-shadow: 0 10px 40px -10px rgba(0, 0, 0, 0.5);
    }

    /* Gradient overlay on hover */
    .project-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0; height: 2px;
      background: linear-gradient(90deg, var(--accent-primary), var(--accent-secondary));
      opacity: 0;
      transition: 0.3s;
    }

    .project-card:hover::before { opacity: 1; }

    .project-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 1.5rem;
    }

    .project-title {
      font-size: 1.5rem;
      color: var(--text-main);
      margin-bottom: 0.5rem;
    }

    /* New style for the visual external icon */
    .external-icon {
      color: var(--text-muted);
      font-size: 1.2rem;
      transition: 0.3s ease;
    }
    
    .project-card:hover .external-icon { 
      color: var(--accent-primary); 
      transform: translate(2px, -2px); /* Subtle movement */
    }

    .project-desc {
      color: var(--text-muted);
      margin-bottom: 2rem;
      font-size: 1rem;
    }

    .feature-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 1rem;
      margin-top: 1.5rem;
    }

    .feature-item {
      background: rgba(255,255,255,0.03);
      padding: 0.8rem 1rem;
      border-radius: 6px;
      font-size: 0.9rem;
      color: #e0e0e0;
      border: 1px solid transparent;
      transition: 0.3s;
    }
    
    /* Ensure hover effects on inner items work despite being inside an anchor */
    .project-card:hover .feature-item {
      border-color: rgba(255, 255, 255, 0.1);
    }
    
    .feature-item:hover {
      border-color: rgba(0, 255, 157, 0.2) !important;
      background: rgba(0, 255, 157, 0.05);
    }

    .tech-tags {
      display: flex;
      gap: 0.8rem;
      margin-bottom: 1.5rem;
      flex-wrap: wrap;
    }

    .tag {
      font-family: var(--font-code);
      font-size: 0.75rem;
      color: var(--accent-secondary);
      background: rgba(0, 184, 255, 0.1);
      padding: 0.25rem 0.6rem;
      border-radius: 4px;
    }

    /* --- FOOTER --- */
    footer {
      text-align: center;
      padding: 4rem 0;
      color: var(--text-muted);
      font-size: 0.9rem;
      border-top: 1px solid var(--border-color);
      margin-top: 4rem;
    }

    /* --- MEDIA QUERIES --- */
    @media (max-width: 768px) {
      .hero {
        grid-template-columns: 1fr;
        padding-top: 8rem;
        gap: 3rem;
        text-align: center;
      }

      .hero-content h1 { font-size: 2.5rem; }
      
      .hero-subtitle { margin: 0 auto 2rem; }
      
      .cta-group { justify-content: center; }

      .nav-wrapper {
        flex-direction: column;
        gap: 1rem;
      }
      
      nav { gap: 1rem; }

      .terminal-window {
        max-width: 400px;
        margin: 0 auto;
      }
    }
  </style>
</head>
<body>

  <header>
    <div class="container nav-wrapper">
      <a href="#" class="logo">Aswanidev<span>_VS</span></a>
      <nav>
        <a href="https://github.com/Aswanidev-vs">GitHub</a>
        <a href="https://www.linkedin.com/in/aswanidev-vs-88738737a">LinkedIn</a>
        <a href="resume.html">Resume</a>
        <a href="download.html" class="btn-nav">Downloads</a>
      </nav>
    </div>
  </header>

  <main>
    <div class="container">
      <!-- HERO SECTION -->
      <section class="hero">
        <div class="hero-content">
          <div class="role-badge">&lt;Backend Architect /&gt;</div>
          <h1>Building scalable systems with code.</h1>
          <p class="hero-subtitle">
            I'm a Go Developer & Vue.js Explorer crafting high-performance CLI tools, APIs, and efficient backend solutions.
          </p>
          <div class="cta-group">
            <a href="#projects" class="btn-primary">View Projects</a>
            <a href="https://github.com/Aswanidev-vs" class="btn-outline">Github Profile</a>
          </div>
        </div>

        <!-- DECORATIVE TERMINAL -->
        <div class="terminal-window">
          <div class="terminal-header">
            <div class="dot red"></div>
            <div class="dot yellow"></div>
            <div class="dot green"></div>
          </div>
          <div class="terminal-body">
            <span class="code-line comment">// Initializing developer profile...</span>
            <span class="code-line"><span class="kw">package</span> main</span>
            <br>
            <span class="code-line"><span class="kw">func</span> <span class="fn">main</span>() {</span>
            <span class="code-line">&nbsp;&nbsp;user := <span class="kw">User</span>{</span>
            <span class="code-line">&nbsp;&nbsp;&nbsp;&nbsp;Name: <span class="str">"Aswanidev VS"</span>,</span>
            <span class="code-line">&nbsp;&nbsp;&nbsp;&nbsp;Stack: []<span class="kw">string</span>{<span class="str">"Go"</span>, <span class="str">"Vue"</span>, <span class="str">"SQL"</span>},</span>
            <span class="code-line">&nbsp;&nbsp;&nbsp;&nbsp;Status: <span class="str">"Ready to Code"</span></span>
            <span class="code-line">&nbsp;&nbsp;}</span>
            <span class="code-line">&nbsp;&nbsp;<span class="fn">Print</span>(user)<span class="cursor"></span></span>
            <span class="code-line">}</span>
          </div>
        </div>
      </section>
    </div>

    <!-- TECH STACK STRIP -->
    <div class="tech-scroller">
      <div class="container tech-list">
        <div class="tech-item">Golang</div>
        <div class="tech-item">Vue.js</div>
        <div class="tech-item">PostgreSQL</div>
        <div class="tech-item">Docker</div>
        <div class="tech-item">REST APIs</div>
        <div class="tech-item">CLI Tools</div>
        <div class="tech-item">Git</div>
      </div>
    </div>

    <!-- PROJECTS SECTION -->
    <div class="container" id="projects">
      <h2 class="section-title">Featured Work</h2>
      
      <!-- 
        CHANGE: Switched from <div> to <a> tag.
        The whole card is now the link.
      -->
      <a href="https://github.com/Aswanidev-vs/WriteDown" target="_blank" class="project-card">
        <div class="project-header">
          <div>
            <h3 class="project-title">WriteDown</h3>
            <div class="tech-tags">
              <span class="tag">Golang</span>
              <span class="tag">CLI</span>
              <span class="tag">System I/O</span>
            </div>
          </div>
          <div class="external-icon">
            <!-- Visual Arrow -->
            ↗
          </div>
        </div>

        <p class="project-desc">
          A lightning-fast, terminal-based note-taking application. Designed for developers who live in the command line, WriteDown offers instant startup times and a keyboard-driven workflow without the bloat of GUI applications.
        </p>

        <div class="feature-grid">
          <div class="feature-item">⚡ Instant Startup</div>
          <div class="feature-item">🖥️ Zero-Dependency</div>
          <div class="feature-item">🔄 Cross-Platform</div>
          <div class="feature-item">⌨️ Vim-like Navigation</div>
        </div>
      </a>
    </div>

  </main>

  <footer>
    <div class="container">
      <p>© 2025 Aswanidev VS. Crafted with Go & Caffeine.</p>
    </div>
  </footer>

</body>
</html>